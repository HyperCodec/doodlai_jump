[package]
name = "game"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[features]
default = []
render = ["bevy/2d"]

[dependencies]
bevy.workspace = true
rand = "0.10.0"
